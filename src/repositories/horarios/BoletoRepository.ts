// D8 CU3 — Acceso a datos para la entidad Boleto
import type { Boleto } from '@/models/horarios/Boleto'

export class BoletoRepository {

    // Stub — persiste todos los boletos generados para un horario
    saveAll(boletos: Boleto[]): void {
        void boletos
    }
}
