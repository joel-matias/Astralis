// D8 CU3 — Genera múltiples horarios a partir de un horario base con recurrencia
import type { Horario } from '@/models/horarios/Horario'
import type { HorarioDTO } from '@/models/horarios/HorarioDTO'

export class RecurrenciaService {

    // Stub — genera instancias de horario según frecuencia; la lógica real depende de FrecuenciaHorario
    generarMultiples(base: HorarioDTO): Horario[] {
        void base
        return []
    }
}
