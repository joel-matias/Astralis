// D8 CU4 — paquete Logica de Negocio; aplica descuentos según tipo de pasajero o promoción
export class GestorDescuentos {
    calcularDescuento(precioBase: number, tipo: string): number { return 0 }
    aplicarPromocion(codigo: string): number { return 0 }
}
